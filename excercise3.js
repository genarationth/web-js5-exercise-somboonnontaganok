let x = 5;
while (x > 0)
{
    x -= 0.5;
    console.log(x);
}

let y = 0;
while (y < 100) 
{
    y += 1;
    if (y % 2 != 0) {
        console.log(y); 
    }
}

let sum = 0;
let n = 0;
while (n < 6) 
{
   
    sum += n;
    n++;
    
}
console.log(sum);

let b = 0;
while (b < 5) {
    b++;
    console.log([b]);
}